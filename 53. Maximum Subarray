class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        num_sum=nums[0]
        max_sum=nums[0]
        for i in range(1,len(nums)):
            num_sum=max(num_sum+nums[i],nums[i])
            max_sum=max(num_sum,max_sum)
        return max_sum
