class Solution:
    def uniqueOccurrences(self, arr: List[int]) -> bool:
        result=[]
        arr_count=Counter(arr)
        for i in arr_count:
            if arr_count[i] in result:
                return False
            result.append(arr_count[i])
        return True
