class Solution:
    def addDigits(self, num: int) -> int:
        while num>9:
            summ=0
            while num>0:
                num,pop=divmod(num,10)
                summ+=pop
            num=summ
        return num
