class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        index=0
        for i in range(len(nums)):
            if index==0 or nums[i]!=nums[index-1]:
                nums[index]=nums[i]
                index+=1
        return index
