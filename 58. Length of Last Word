class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        word_count=0
        s=s.strip()
        for i in range(len(s)-1,-1,-1):
            if s[i].isalnum():
                word_count+=1
            else:
                break
        return word_count
